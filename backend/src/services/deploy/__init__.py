"""V4 Part 3 — auto-deploy: turning an auto-approved app into a live Container App
with no human step, driven by the admin-invoked `deploy-reconcile` endpoint rather
than an in-process scheduler (this codebase has none — see `provision.py`)."""
