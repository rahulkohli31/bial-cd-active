"""The super-admin governance domain: app-registry lifecycle governance (U8) and
per-user limit management + feedback read (U9). Every endpoint is `requires_superadmin`
-gated and audited (ADR-0005)."""
