# ── Claude relay (existing) ───────────────────────────────────────────────
# Server-side only. Keep the Foundry key out of the browser bundle.
ANTHROPIC_FOUNDRY_API_KEY=
AZURE_FOUNDRY_RESOURCE_NAME=
PORT=3001

# ── PowerPoint (.pptx) deck chat attachments (Gotenberg -> PDF -> Claude) ──
# DARK by default. The .pptx path turns on ONLY when BOTH are set: the flag is
# the literal string 'true' AND GOTENBERG_URL points at a reachable conversion
# sidecar. With either missing, .pptx uploads are rejected with a clear message
# (never a 500); the composer also hides .pptx behind a client flag
# (src/config/features.js -> DECK_ATTACHMENTS_ENABLED). Flip BOTH to enable.
#
# A deck is a VISUAL medium, so it is NOT text-extracted like .docx/.xlsx: the
# sidecar renders it to a PDF, the PDF is uploaded to the Anthropic Files API
# THROUGH Foundry (beta files-api-2025-04-14), and Claude reads it with vision.
# The model never receives raw .pptx bytes; the original .pptx is kept for
# re-download. Reuses ANTHROPIC_FOUNDRY_API_KEY/AZURE_FOUNDRY_RESOURCE_NAME above.
DECK_ATTACHMENTS_ENABLED=false
# Base URL of the in-tenant Gotenberg (LibreOffice) sidecar. Local dev: the
# docker-compose `gotenberg` service. Production: an in-tenant, locked-down
# sidecar (ops/gotenberg/README.md). Confidential decks NEVER egress the tenant.
GOTENBERG_URL=http://localhost:3000
# Max pages in the converted PDF (cost + model governor). Over-cap = a clean 413,
# nothing stored. Default 100; the 1M-context Opus model supports up to 600
# (200k-context models cap at 100) — raise only if you accept the per-deck cost.
MAX_DECK_PAGES=100
# Conversion wall-clock budget in ms for the Gotenberg render. Over-budget aborts
# with a clean 504 (nothing stored). Align the sidecar's own `--api-timeout` at or
# below this so neither side hangs. Default 60000 (60s).
DECK_CONVERT_TIMEOUT_MS=60000

# ── Cosmos DB for MongoDB (interim auth, shared POC account) ──────────────
# The shared account is a Cosmos *for MongoDB* (RU) account, so we connect with
# the MongoDB driver, NOT the NoSQL/Core SDK. Get the connection string from the
# Azure portal: Cosmos account -> Settings -> Connection strings (it already
# includes ssl=true and the REQUIRED retrywrites=false). The database and the
# `users` collection are pre-created in Data Explorer (see README/setup notes).
MONGODB_URI=mongodb://<account>:<key>@<account>.mongo.cosmos.azure.com:10255/?ssl=true&retrywrites=false&replicaSet=globaldb
MONGODB_DATABASE=citizen_portal
MONGODB_USERS_COLLECTION=users
# Daily per-user token usage counter (the ONLY new persistence). Pre-create this
# collection in Data Explorer alongside `users`; the code never creates it.
MONGODB_USAGE_COLLECTION=daily_token_usage
# User-feedback submissions (append-only). Like the others, pre-create this
# collection in Data Explorer (Cosmos) before deploy; the code never creates it.
# Local dev (the `mongo:7` docker container) auto-creates it on first write, so
# only this env var is needed locally.
MONGODB_FEEDBACK_COLLECTION=feedback
# ── Persistence: chats, messages, attachment quota (server-side, per-user) ──
# Three new collections move a user's work out of the browser and onto the
# server so it follows them across browsers/devices. Pre-create all three in
# Data Explorer (Cosmos) before deploy; the code only resolves handles, never
# creates collections. Local dev (`mongo:7`) auto-creates on first write.
#
# Required composite indexes are created AUTOMATICALLY at boot by
# server/ensure-indexes.js (idempotent) — or run `node scripts/ensure-indexes.js`
# to apply them to a live account without a redeploy. Cosmos for MongoDB 400s a
# filter-on-A + sort-on-B (or multi-field sort) query without one; local Mongo
# does not need them. ensure-indexes.js is the single source of truth, currently:
#   conversations: { username:1, updatedAt:-1 } AND { username:1, kind:1, updatedAt:-1 }
#   messages:      { conversationId:1, username:1, seq:1 }  (single-field sort only — Cosmos 400s a multi-field ORDER BY)
#   feedback:      { createdAt:-1 }
# Partition key (Cosmos): /username on conversations + messages, /_id on
# attachment_usage — matches the per-user read pattern.
#
# conversations: one lightweight HEADER doc per conversation (chat + builder),
# carrying the builder's generated `code.current`. messages: one doc per message
# with a structured `parts[]` array (no unbounded arrays on a growing doc).
# attachment_usage: one doc per user (`_id` = username) with the running byte
# total for the per-user attachment quota.
MONGODB_CONVERSATIONS_COLLECTION=conversations
MONGODB_MESSAGES_COLLECTION=messages
MONGODB_ATTACHMENT_USAGE_COLLECTION=attachment_usage

# ── Dynamic app data service (App Registry + shared CRUD + audit) ──────────
# Three new collections back generated apps that read/write REAL data instead of
# fabricated placeholders. Pre-create all three in Data Explorer (Cosmos) before
# deploy; the code only resolves handles, never creates collections. Local dev
# (`mongo:7`) auto-creates on first write. Enable Server-Side Retry on the
# account; keep retrywrites=false in MONGODB_URI.
#
#   app_registry: one doc per app (`_id` = the builder conversation uuid) — app
#     key (a publishable scoping label, NOT a secret), loginRequired flag,
#     deploy status (draft|pending|approved|rejected|disabled), code snapshots
#     (source + approvedSnapshot), pinned dataSchema, and per-app quota counters.
#     Index { appKey: 1 } (key lookup) and { status: 1 } (admin list).
#   data_records: the generic schemaless per-app record store — one doc per
#     record carrying an arbitrary `data` object. Partition key /appId; index
#     { appId: 1, collection: 1, createdAt: -1 } (per-app newest-first reads).
#   audit_logs: append-only who-did-what (data mutations + admin actions); never
#     stores record contents. Index { appId: 1, at: -1 }.
#   app_files: per-app FILE metadata — one doc per uploaded file carrying
#     filename/contentType/size/blobKey + a pending|ready status (the bytes live in
#     the object store, never here). Partition key /appId; TWO indexes created at
#     boot by ensure-indexes.js: { appId:1, createdAt:-1 } and
#     { appId:1, collection:1, createdAt:-1 } (single-field ORDER BY only on Cosmos).
MONGODB_APP_REGISTRY_COLLECTION=app_registry
MONGODB_DATA_RECORDS_COLLECTION=data_records
MONGODB_AUDIT_COLLECTION=audit_logs
MONGODB_APP_FILES_COLLECTION=app_files

# ── Object store (attachment bytes) ───────────────────────────────────────
# Attachment BYTES (image/PDF, ≤4 MB) live here, NEVER in the metadata DB — so
# the imminent Cosmos→Postgres migration leaves this layer untouched. Objects
# are written under per-user keys `att/<username>/<attachmentId>`, so a GET
# derived from the JWT can only ever read the caller's own namespace. The
# bucket/container is a deploy prerequisite (the code never creates it).
#
# Pick the backend with OBJECT_STORE_PROVIDER:
#   azure → Azure Blob Storage (native SDK @azure/storage-blob). NOTE: Azure Blob
#           is NOT S3-compatible — it needs this provider, not the S3 client.
#   s3    → any S3-compatible store (AWS S3 / MinIO / R2 / B2 / Spaces / …).
# OBJECT_STORE_BUCKET is the bucket name (s3) or container name (azure).
OBJECT_STORE_PROVIDER=azure
OBJECT_STORE_BUCKET=bial-attachments

# Azure Blob (provider=azure). Storage account → Access keys → Connection string.
# Local dev: the Azurite emulator's well-known devstoreaccount1 string.
AZURE_STORAGE_CONNECTION_STRING=

# S3-compatible (provider=s3). REGION is optional (MinIO ignores it; the SDK
# requires some value — default us-east-1).
OBJECT_STORE_ENDPOINT=http://minio.internal:9000
OBJECT_STORE_ACCESS_KEY=
OBJECT_STORE_SECRET_KEY=
OBJECT_STORE_REGION=us-east-1

# ── Per-app file/blob storage (download SAS + proxy upload) ────────────────
# Generated apps can persist FILES (the original upload and/or a generated
# report), not just records. Bytes live in OBJECT_STORE_BUCKET under per-app keys
# `apps/<appId>/<fileId>` (reuses the bucket above — no new container needed).
# Uploads stream THROUGH the backend (so type/size/quota are enforced at the
# gateway); downloads are offloaded via a short-lived, read-only, single-blob,
# IP-scoped SAS the backend mints (the blob host is NEVER added to the sandbox CSP).
#
# SAS download lifetime (seconds). Short by design — a leaked URL is usable only
# for this window.
FILE_SAS_TTL_SECONDS=120
# The BIAL egress IP (or range `start-end`) the download SAS is restricted to, so
# a leaked URL is unusable off-network. STRONGLY recommended for go-live: pair it
# with disabling public network access on the storage account. If unset, the SAS
# is minted with NO IP scope (a logged warning). Azure only; ignored by S3/MinIO.
FILE_SAS_SIGNED_IP=
# express.json body-parser limit for the /api/apps/:appId/files upload route
# (base64 inflates bytes ~37%, so this is larger than APP_FILE_MAX_BYTES).
APP_FILE_MAX_JSON=25mb
# Max DECODED file size per upload (checked after base64 decode), in bytes (≈18 MB).
APP_FILE_MAX_BYTES=18874368
# Per-app file quota: total bytes (≈500 MB) and total file count. Separate from the
# record quota so files and records don't starve each other.
APP_FILE_BYTES_CAP=524288000
APP_FILE_COUNT_CAP=2000
# Comma-separated allowlist of upload content-types. SVG is intentionally EXCLUDED
# (script-execution vector, not reliably magic-validatable). Leave unset to use the
# built-in default (csv/xlsx/xls/json/txt/pdf/png/jpeg/gif/webp).
APP_FILE_ALLOWED_TYPES=

# ── Daily token limit (server-enforced) ───────────────────────────────────
# Flat per-user/day ceiling enforced in the /api/claude proxy. Real input+output
# token counts (incl. cache tokens) are captured from the Anthropic stream and
# accumulated per username + IST-date; over-limit requests get a 429 pre-stream.
DAILY_TOKEN_LIMIT=1000000

# ── Password login decommission gate (Entra ID is the way in) ─────────────
# The POC username/password login is being retired in favour of backend-owned
# Entra ID sign-in (FastAPI control-plane, ADR-0007). Leave ENABLED (default)
# until the FastAPI Entra path is verified live in production, then set to
# `false` (or `0`) to reject new password logins AND refreshes the MOMENT Entra
# works — closing the Entra-offboarding-bypass window, decoupled from the later
# physical route-removal deploy. Logout stays open regardless.
PASSWORD_LOGIN_ENABLED=true

# ── JWT / token lifetimes ─────────────────────────────────────────────────
# JWT_SECRET MUST be a >=64-char CSPRNG value, shared with the future FastAPI
# service. Generate one with:  node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=
# Access token TTL (short-lived). Accepts any `ms`/jsonwebtoken duration string.
ACCESS_TOKEN_TTL=15m
# Refresh token TTL (long-lived opaque token).
# Format: <number><s|m|h|d> only (e.g. 7d, 168h, 60m). Other formats (e.g.
# "7 days", "1w") are rejected at startup rather than silently defaulting to 7d.
REFRESH_TOKEN_TTL=7d

# ── Rate limiting / proxy trust ───────────────────────────────────────────
# TRUST_PROXY MUST equal the real number of proxy hops that append to
# X-Forwarded-For (1 for a single load balancer; 2 for e.g. Front Door + App
# Service) so req.ip is the real client IP. A wrong value either lets clients
# spoof their IP or collapses every client to the LB address — which makes the
# per-IP limiter below lock out everyone behind that proxy. Default 1.
TRUST_PROXY=1
# Per-IP ceiling on auth attempts (login + refresh) per 15 min — bounds cross-
# username spraying and the Argon2 cost amplifier. Set to 0 to DISABLE when
# clients genuinely share one ingress IP (corporate NAT); the per-username:IP
# limiters still apply. Default 60.
AUTH_IP_RATE_LIMIT=60
