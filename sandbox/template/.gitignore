# Baked into the image at build time — never commit these.
/node_modules
/.next
/out
/build

# Local env (the injected BIAL_* vars arrive at provision — see .env.example). A values file must
# never enter the C4 git-bundle snapshot, so .env*.local stays ignored as a backstop.
.env
.env.local
.env*.local

# Transient restore artifact — restore.sh's caller writes the base64 bundle here and an EXIT trap
# removes it. Gitignored so a trap bypassed by SIGKILL (e.g. the /exec timeout-kill) can never let a
# later `git add -A` commit the multi-MB base64 into a future snapshot bundle.
/.bial-restore.b64

# Same hazard, production restore path: the C4 restore writes the base64 bundle into the worktree
# and only `rm`s it as its last step — a restore killed mid-way would leave it for the next
# snapshot to commit.
/app.bundle
/app.bundle.b64

# NOT ignored, on purpose: /drizzle. The generated migration SQL (and its meta/ journal) is a
# VERSIONED ARTIFACT, not build output — it is the only record of how the database reached its
# current shape. Ignoring it would make a snapshot restore come back with code that expects
# tables and a database that has none. Never add a `drizzle/` entry here.

# Logs / debug
npm-debug.log*
*.tsbuildinfo
