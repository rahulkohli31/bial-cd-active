/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited — Next.js regenerates it (and may add typed-route refs)
// on `next dev` / `next build`. It is committed here so `npm run lint` (tsc --noEmit) resolves
// Next's ambient types (including the CSS side-effect import in app/layout.tsx) before the dev
// server has run.
