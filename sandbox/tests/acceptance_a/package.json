{
  "name": "bial-acceptance-a",
  "private": true,
  "type": "module",
  "description": "Track SANDBOX acceptance (a): drives a real Chromium against the golden template running on the real image to prove a CRUD round-trip (plan U13). Self-contained so it does not depend on portal/ being installed (R8: no portal edits). Pinned to match portal's @playwright/test ^1.61.1.",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "acceptance": "node run.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
