{
  "name": "bial-flight-data-seed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Verification workspace for the golden template's flight-data reference file. Nothing here ships and Dockerfile.sandbox never copies it: flight-data.ts is typechecked under the template's own strict settings and unit-tested HERE, because the template pre-installs none of the four packages it imports, then rendered into template/lib/flight-data.reference.ts as line comments by generate-reference.mjs, whose docblock is this workspace's documentation.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "generate": "node generate-reference.mjs"
  },
  "dependencies": {
    "@azure/identity": "4.13.2",
    "@azure/storage-blob": "12.33.0",
    "hyparquet": "1.30.0",
    "hyparquet-compressors": "1.1.1"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
